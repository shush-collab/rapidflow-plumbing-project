/**
 * 21ST.DEV COMPONENT EXAMPLE
 * 
 * This is a placeholder file to demonstrate how to add components from 21st.dev.
 * 
 * To add a 21st.dev component:
 * 1. Visit https://21st.dev/community/components
 * 2. Browse the component library
 * 3. Click on a component to see its code
 * 4. Copy the component code
 * 5. Create a new file in this folder (e.g., MagneticCard.tsx)
 * 6. Paste the code and adjust to your needs
 * 7. Make sure all dependencies are installed (framer-motion, @tabler/icons-react)
 * 
 * Example component structure:
 */

import { motion } from 'framer-motion';

// This is just an example - replace with actual 21st.dev component
export default function Example21stDevComponent() {
    return (
        <motion.div
            whileHover={{ scale: 1.05 }}
            className="p-4 border rounded-lg"
        >
            <p className="text-muted-foreground">
                Replace this with your 21st.dev component code
            </p>
        </motion.div>
    );
}

/**
 * POPULAR 21ST.DEV COMPONENTS FOR YOUR SITE:
 * 
 * - Magnetic Buttons: Buttons that follow cursor movement
 * - 3D Card Tilt: Cards with 3D perspective on hover
 * - Animated Badges: Floating badges with animations
 * - Gradient Borders: Animated gradient card borders
 * - Text Reveal: Text that reveals on scroll
 * - Particle Backgrounds: Animated particle effects
 * - Glow Effects: Neon glow on hover
 * - Morphing Shapes: SVG shape morphing animations
 */
